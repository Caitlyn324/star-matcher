<h1>Star Matcher</h1>
<% @auditions.each do |aud| %>
  <h2><%= link_to aud.show, audition_path(aud) %></h2>
  <h4><%= aud.roles.size %> role(s) available</h4>
  <p><%= aud.description %></p>
<% end %>
